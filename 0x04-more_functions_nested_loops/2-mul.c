#include "main.h"

/**
 * mul - multiplies two integers
 * @a : character to check the case
 * @b : character to check the case
 * Return: result of a * b
 */
int mul(int a, int b)
{

	return (a * b);

}
