#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{

	int _number;

	for (_number = 0; _number < 10; _number++)

		putchar((_number));

	putchar('\n');

	return (0);
}